<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.horoscope" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-sdk android:minSdkVersion="14"
        android:targetSdkVersion="17"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/edition">
    <activity
        android:name="com.example.user.horoscope.Splash_Screen"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@android:style/Theme.NoTitleBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>


        <activity
            android:name="com.example.user.horoscope.MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"></activity>

        <activity
            android:name="com.example.user.horoscope.News"

            android:theme="@android:style/Theme.NoTitleBar">

        </activity>
        <activity
            android:name="com.example.user.horoscope.Categories"

            android:theme="@android:style/Theme.NoTitleBar">

        </activity>
        <activity
            android:name="com.example.user.horoscope.SingleActivity"
            android:theme="@android:style/Theme.NoTitleBar">

        </activity>
        <activity android:name=".Another"
            android:theme="@android:style/Theme.NoTitleBar"
            >

            </activity>
        <activity android:name=".Shows2"
            android:theme="@android:style/Theme.NoTitleBar"
            ></activity>
        <activity android:name=".shows3"
            android:theme="@android:style/Theme.NoTitleBar"
            ></activity>
        <activity android:name=".new1"
            android:theme="@android:style/Theme.NoTitleBar"
            ></activity>




        <activity android:name=".Shows1"
            android:theme="@android:style/Theme.NoTitleBar"
            ></activity>

        <activity android:name=".english"
            android:theme="@android:style/Theme.NoTitleBar"
            ></activity>
    </application>

</manifest>
